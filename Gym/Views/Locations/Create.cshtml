@{
  Layout = "_Layout";
}

<h1>Create A new Gym Location!</h1>
@model Gym.Models.Location
@using (Html.BeginForm())
{
  @Html.LabelFor(model=>model.LocationName)
  @Html.EditorFor(model=>model.LocationName)
  <button>Save!</button>
}